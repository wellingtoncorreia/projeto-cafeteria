import { MoneyFormatDirective } from './money-format.directive';

describe('MoneyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
